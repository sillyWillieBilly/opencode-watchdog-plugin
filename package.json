{
  "name": "opencode-watchdog-plugin",
  "version": "0.1.0",
  "description": "OpenCode plugin that evaluates tool calls through a safety watchdog pipeline.",
  "main": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist",
    "configure": "bun run src/cli/configure.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.4.0",
    "@opencode-ai/sdk": "1.4.0"
  },
  "devDependencies": {
    "@inquirer/prompts": "^8.4.1",
    "bun-types": "1.3.12",
    "typescript": "^5.9.3"
  }
}
